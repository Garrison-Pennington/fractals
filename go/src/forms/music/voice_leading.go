package music

// Voice leading functions which do the same thing but with various signatures
// We always voice lead from a starter chord, but can use a few types as the
// second parameter such as: Tone, Note, Chord
func CommonToneVoiceLead(start Chord, next Tone) (lead Chord) {
	return
}
